<view class="container">
  <view class="info">{{t('code_prompt')}}</view>
  <input class="input" type="number" placeholder="{{t('code_prompt')}}" value="{{otp}}" bindinput="onOtpInput" />
  <button type="primary" loading="{{verifying}}" bindtap="verifyCode">{{t('verify')}}</button>
</view>