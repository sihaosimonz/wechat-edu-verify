<view class="container">
  <view class="info">{{ui.code_prompt}}</view>
  <input class="input" type="number" placeholder="{{ui.code_prompt}}" value="{{otp}}" bindinput="onOtpInput" />
  <button type="primary" loading="{{verifying}}" bindtap="verifyCode">{{ui.verify}}</button>
</view>
